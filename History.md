## 概要一覧 (Summary Table)

| **_ver_** | **_リリース概要_**                         |
| --------- | ------------------------------------------ |
| 1.2.0     | 【機能追加】パターン名・複写機能・最大 50 件制限・全角空白対応 trim・誤操作防止確認ダイアログ |
| 1.1.0     | 【機能追加】アクティブ／ホバー時色指定・透過度対応・フォントプレビュー等 |
| 1.0.0     | 【新規作成】URL パターンに基づくタブ色付け機能リリース |

## 各バージョンの詳細 (Release Notes)

-   **1.2.0 【機能追加】パターン名・複写機能・最大 50 件制限・全角空白対応 trim・誤操作防止確認ダイアログ**

    -   ルールに「パターン名」を追加。識別用の任意名（最大 50 文字）をルール一覧の 1 段目に表示する。既存ルールのパターン名は空欄で引き継ぎ、編集時に入力が必要（後方互換）。
    -   パターン名のバリデーション（必須・50 文字以下・重複不可）を実装。重複判定は前後の全角・半角空白を除去後、大文字小文字を区別せずに行う。
    -   ルール一覧に「複写」ボタンを追加（`[編集] [複写] [削除]` の順）。既存ルールの全項目をフォームに複写し、新規登録の準備状態にする。パターン名はそのまま複写されるため、登録時に重複エラーで別名への変更を促す。更新モード中に複写ボタンを押下した場合は、確認ダイアログ（入力中変更の破棄）を経て追加モードへ切り替わる。
    -   ルール登録件数の上限を 50 件に設定。50 件到達時は `[新規登録]` でエラー表示、複写ボタンを `disabled` にする。51 件以上が保存された状態で起動した場合は警告ダイアログを表示後、51 件目以降を強制削除して永続化する。
    -   ルール一覧テーブルのヘッダを 2 段化（「パターン名 / URL パターン」「フォント色 / アクティブ／ホバー」「背景色 / アクティブ／ホバー」）。1 段目と 2 段目は同じフォントスタイルで表示し、間に水平線を引いて視認性を確保。
    -   ルール一覧の「フォント色」「背景色」セル内にも 1 段目（通常時）と 2 段目（アクティブ／ホバー時）の間に水平線を追加。片方のみ値が入っている場合でも、どちらに値が設定されているかが一目で識別可能。
    -   テーブル幅を `max-width: min(1600px, 95vw)` / `min-width: 720px` にレスポンシブ化。4K モニタでより広く使用でき、狭いウィンドウでは横スクロールを許容。
    -   URL パターン等の保存時 trim 処理を全角空白（U+3000）対応の `TrimAll` に統一。従来の半角空白のみ除去する挙動を改善し、URL パターンに前後全角空白を含めて入力した場合も保存値から除去されるようになった。
    -   ライトテーマの背景色を `#fafafa` から `#eee` に調整し、明るすぎる印象を緩和。
    -   ルール一覧の各ボタン操作時の確認ダイアログを統一・強化：
        -   `[編集]`／`[複写]` ボタン：入力中の変更内容がある場合（formDirty=true）に「入力中の内容は破棄されます。よろしいですか？」を表示。
        -   `[削除]` ボタン：誤クリック防止のため formDirty に関わらず常に確認ダイアログを表示（ルール名または URL パターンを併記）。formDirty=true の場合は入力破棄＋削除を一文に統合した文言で表示。
        -   既存の入力破棄確認 i18n キー `confirmDiscardForCopy` は `confirmDiscardChanges` へ改名し、編集／複写で共用化。新規キー `confirmDeleteRule` / `confirmDeleteRuleAndDiscard` を追加。

-   **1.1.0 【機能追加】アクティブ／ホバー時色指定・透過度対応・フォントプレビュー等**

    -   ルールに「アクティブ／ホバー時フォント色」「アクティブ／ホバー時背景色」を追加。いずれも任意指定で、未指定時は従来通りの自動反転挙動にフォールバック。
    -   色指定に透過度（α）を対応。受理形式は `#rgb` / `#rgba` / `#rrggbb` / `#rrggbbaa` の 4 種類。オプション画面に α スライダーを追加し、テキスト欄・カラーピッカーと双方向連動する。
    -   ルール設定画面にフォントプレビューエリアを追加。フォント入力時のみ、指定フォントでのサンプル文（英数／かな／カナ／記号）表示で視覚確認可能。
    -   ルール一覧の「フォント色」「背景色」セルを 2 段構成へ変更し、通常色／アクティブ色をひと目で把握可能に。α 付き色はチェッカー背景で透過であることを視覚化。
    -   ルール一覧の「URL パターン」「フォント」列に title 属性を付与。列幅超過で省略表示された文字列をマウスホバーで全文確認可能。
    -   v1.1.0 の追加仕様書を `docs/040_v1.1.0_追加仕様書.md` として新設。以降のバージョンでも差分仕様書を同形式で追加する方針。

-   **1.0.0 【新規作成】URL パターンに基づくタブ色付け機能リリース**

    -   TST (Tree Style Tab) External API を利用し、サイドバー上のタブへフォント・フォント色・背景色を適用する機能を実装。
    -   URL パターンの種別として「前方一致」「正規表現」の 2 種類をサポート。
    -   ルール設定画面（オプションページ）を実装。
        -   ルールの追加・編集・削除・並び替え操作に対応。
        -   入力値のバリデーション（URL パターン未入力／正規表現構文エラー／カラーコード形式エラー／全スタイル未指定）に対応。
        -   ライト／ダークのテーマ切替機能を搭載。
    -   タブの新規作成・URL 変更・TST 再ロードに追従するリアルタイム反映を実装。
    -   全タブ一括適用を高速化するバッチ送信ロジック（分類 + 一括 `remove-tab-state` + ルール別 `add-tab-state`）を実装。<br>
        参考値：作者の環境で約 2,000 タブを 0.25 秒で適用完了。
    -   進捗表示用のポップアップを実装。
        -   分類中／旧クラス削除中／ルール適用中／完了 の各ステージを進捗バーとテキストで表示。
        -   オプションページでも同様の進捗を表示。
