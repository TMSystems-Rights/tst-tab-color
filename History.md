## 概要一覧 (Summary Table)

| **_ver_** | **_リリース概要_**                         |
| --------- | ------------------------------------------ |
| 1.1.0     | 【機能追加】アクティブ／ホバー時色指定・透過度対応・フォントプレビュー等 |
| 1.0.0     | 【新規作成】URL パターンに基づくタブ色付け機能リリース |

## 各バージョンの詳細 (Release Notes)

-   **1.1.0 【機能追加】アクティブ／ホバー時色指定・透過度対応・フォントプレビュー等**

    -   ルールに「アクティブ／ホバー時フォント色」「アクティブ／ホバー時背景色」を追加。いずれも任意指定で、未指定時は従来通りの自動反転挙動にフォールバック。
    -   色指定に透過度（α）を対応。受理形式は `#rgb` / `#rgba` / `#rrggbb` / `#rrggbbaa` の 4 種類。オプション画面に α スライダーを追加し、テキスト欄・カラーピッカーと双方向連動する。
    -   ルール設定画面にフォントプレビューエリアを追加。フォント入力時のみ、指定フォントでのサンプル文（英数／かな／カナ／記号）表示で視覚確認可能。
    -   ルール一覧の「フォント色」「背景色」セルを 2 段構成へ変更し、通常色／アクティブ色をひと目で把握可能に。α 付き色はチェッカー背景で透過であることを視覚化。
    -   ルール一覧の「URL パターン」「フォント」列に title 属性を付与。列幅超過で省略表示された文字列をマウスホバーで全文確認可能。
    -   v1.1.0 の追加仕様書を `docs/040_v1.1.0_追加仕様書.md` として新設。以降のバージョンでも差分仕様書を同形式で追加する方針。

-   **1.0.0 【新規作成】URL パターンに基づくタブ色付け機能リリース**

    -   TST (Tree Style Tab) External API を利用し、サイドバー上のタブへフォント・フォント色・背景色を適用する機能を実装。
    -   URL パターンの種別として「前方一致」「正規表現」の 2 種類をサポート。
    -   ルール設定画面（オプションページ）を実装。
        -   ルールの追加・編集・削除・並び替え操作に対応。
        -   入力値のバリデーション（URL パターン未入力／正規表現構文エラー／カラーコード形式エラー／全スタイル未指定）に対応。
        -   ライト／ダークのテーマ切替機能を搭載。
    -   タブの新規作成・URL 変更・TST 再ロードに追従するリアルタイム反映を実装。
    -   全タブ一括適用を高速化するバッチ送信ロジック（分類 + 一括 `remove-tab-state` + ルール別 `add-tab-state`）を実装。<br>
        参考値：作者の環境で約 2,000 タブを 0.25 秒で適用完了。
    -   進捗表示用のポップアップを実装。
        -   分類中／旧クラス削除中／ルール適用中／完了 の各ステージを進捗バーとテキストで表示。
        -   オプションページでも同様の進捗を表示。
